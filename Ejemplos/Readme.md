# Ejemplos - Iniciación a la programación 

## Información

Esta sección contiene los `Ejemplos` del `temario`.

Los directorios/ejercicios se prefijan con el número del `temario`, no `incremental`.

